# EC Backend Development Guidance

## スコープ

- このリポジトリは顧客向けECバックエンド（UC001～UC008）を扱う。
- 管理側バックエンドとフロントエンドは参照のみとし、明示的な依頼なしに変更しない。
- 要件資料、既存コード、既存テストの順に優先し、資料にない仕様は断定しない。

## アーキテクチャ

- .NET 10 / ASP.NET Core Web API / PostgreSQLを使用する。
- Domain、Application、Infrastructure、APIの4層を維持する。
- Controllerへ業務ロジックを書かない。
- APIパスは原則 `/api/ec/...`、ローカルポートは `5100` とする。

## セキュリティとDB

- パスワードを平文保存しない。
- EF Coreのパラメータ化クエリを使用する。
- 顧客認証には管理者認証と異なる `CustomerJwt` 設定を使用する。
- 商品画像は参照のみとする。
- 購入確定では注文、注文明細、在庫更新を同一トランザクションで扱う。
- 在庫不足、存在しない商品、削除済み商品を検証する。

## 作業手順

1. 資料、コード、テスト、DB定義を調査する。
2. UC、API、テーブル、正常系、異常系、認証要件を整理する。
3. 実装前に変更予定ファイルと実装順を提示する。
4. 1機能または1層単位で変更する。
5. 実装後に `dotnet build` と `dotnet test` を実行する。
6. コミット前に対象ファイルを説明する。

## Git

- コミットメッセージは必ず「安藤」で始める。
- 1回の作業で作成するコミットは1つだけとする。
- 明示的な依頼なしにpushしない。
- 他メンバーの変更や個人用実装ログをコミットしない。
- reset、rebase、stash、pullの前に作業ツリーを確認する。
